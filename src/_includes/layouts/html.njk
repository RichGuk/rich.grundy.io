<!DOCTYPE html>
<html lang="en" data-theme="light" {% if forceDark %}class="-force-dark"{% endif %}>
  <head>
    <meta charset="utf-8">
    {% if description %}<meta name="description" content="{{ description }}">{% endif %}
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% if title %}{{ title }} - {{ site.title }}{%- else -%}{{ site.title }}{% endif %}</title>
    <link rel="icon" href="/assets/images/avatar-circle.svg" type="image/svg+xml">
    <link rel="preload" as="image" href="/assets/images/avatar-sunglasses.svg">
    <link rel="preload" as="image" href="/assets/images/avatar.svg">
    <style>
@font-face {
  font-family: 'Catamaran';
  src: local('Catamaran Regular'), local('Catamaran-Regular'),
  url('/assets/fonts/Catamaran-Regular.woff2') format('woff2');
font-weight: normal;
font-style: normal;
font-display: fallback;
}

  @font-face {
    font-family: 'Catamaran';
    src: local('Catamaran Bold'), local('Catamaran-Bold'),
    url('/assets/fonts/Catamaran-Bold.woff2') format('woff2');
  font-weight: bold;
  font-style: normal;
  font-display: fallback;
  }

  body {
    font-family: 'Catamaran',-apple-system,BlinkMacSystemFont,"Segoe UI Variable","Segoe UI",system-ui,ui-sans-serif,Helvetica,Arial,sans-serif,"Apple Color Emoji","Segoe UI Emoji","Noto Color Emoji";
  }
    </style>
    <script>
      const wantsDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
      const storedPreference = localStorage.getItem('theme');
      if (storedPreference == 'dark' || (!storedPreference && wantsDark)) {
        document.documentElement.setAttribute('data-theme', 'dark')
      }
    </script>
    <link rel="stylesheet" href="{% assetDigest 'app.css' %}">
    <script async type="text/javascript" src="{% assetDigest 'app.js' %}"></script>
    <script async defer data-domain="rich.grundy.io" src="https://plausible.grundy.io/js/plausible.js"></script>
    <link type="application/atom+xml" rel="alternate" href="{{ site.feedUrl }}" />
  </head>
  <body>
    {{ content | safe }}
  </body>
</html>
